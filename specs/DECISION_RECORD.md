# Core Rental — Consolidated Decision Record

> Status: **CONFIRMED** (grill-with-docs, 65 decisions) — implementation NOT started (user holds the go signal).
> Source: `docs/design/*.html` mockups + `docs/design/tropical_tech/DESIGN.md` + grilled decisions.
> Companion artifacts: `specs/product/SCOPE_LATEST.yaml`, `specs/tech-architecture/tech-stack.md`, `specs/adr/`.

---

## Product & UX

| #   | Decision               | Resolution                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Framework              | **Next.js only (React, App Router)** — Angular dropped (ADRs 0001)                                                                                                                                                                                                                                                                                                                                     |
| 2   | Languages              | **English only for MVP** — i18n deferred; flat routes kept retrofit-friendly for a `[locale]` group later                                                                                                                                                                                                                                                                                              |
| 3   | Currency               | **IDR only** — `Intl.NumberFormat('id-ID')` display                                                                                                                                                                                                                                                                                                                                                    |
| 4   | Data source            | **Static JSON catalog**, no backend, no server-side persistence                                                                                                                                                                                                                                                                                                                                        |
| 5   | Rent flow              | **Mock confirmation screen** — no payment, no inquiry hand-off                                                                                                                                                                                                                                                                                                                                         |
| 6   | Categories             | **Mockup set:** Chair, Desk, Accessories (monitor/lamp/plant/coffee machine/bean bag), Extras (surfboard), Partner (motorcycle). "Garage space" dropped                                                                                                                                                                                                                                                |
| 7   | Pricing                | **Flat monthly per item** — summary total = sum of selected items (C1: no delivery fee, no grand total)                                                                                                                                                                                                                                                                                                |
| 8   | Dark mode              | **Light mode only** — `dark:` variants not ported                                                                                                                                                                                                                                                                                                                                                      |
| 9   | Images (original)      | Google mockup URLs + `onError` fallback — _superseded by #31_                                                                                                                                                                                                                                                                                                                                          |
| 10  | Quantities             | **Accessories stack via steppers; chair/desk single selection**                                                                                                                                                                                                                                                                                                                                        |
| 11  | Persistence            | **`localStorage` cart** — survives refresh/return visits                                                                                                                                                                                                                                                                                                                                               |
| 18  | Brand                  | **"Core Rental"** — "Moni's Workspace" retired (mockups conflicted)                                                                                                                                                                                                                                                                                                                                    |
| 19  | Catalog model          | **Unified `products.json`**, filtered per view; new products (desks/monitors/lamps) created as data                                                                                                                                                                                                                                                                                                    |
| 20  | Extras semantics       | **Surfboard → cart** (standard line item) · **Motorcycle → display-only** — _superseded:_ the partner request modal was removed (store is now a catalog gallery; adding lives only in the Builder's Selection Panel); partner remains structurally excluded from cart/summary (N6)                                                                                                                     |
| 21  | IDR prices             | **Fresh round market-rate price points**, mockup relative ordering preserved (espresso > chair > plant)                                                                                                                                                                                                                                                                                                |
| 22  | Quantity caps          | **Per-category caps:** monitors ≤ 3, plants ≤ 4, lamps ≤ 2, coffee = 1, bean bag ≤ 2, surfboard = 1                                                                                                                                                                                                                                                                                                    |
| 23  | Empty cart             | **Friendly empty state + "Start building" CTA** on `/summary`                                                                                                                                                                                                                                                                                                                                          |
| 24  | Accessibility          | **Baseline:** semantics, labels, focus-visible, keyboard-operable canvas                                                                                                                                                                                                                                                                                                                               |
| 27  | 404 treatment          | **Playful brand copy** ("this page has surfed away") + Back to Home / Start Building CTAs                                                                                                                                                                                                                                                                                                              |
| 30  | Catalog volume         | **53 products:** 10 chairs · 10 desks · 32 accessories (8 monitors · 6 lamps · 8 plants · 5 coffee · 5 bean bags) · 1 partner motorcycle — extras/surfboards removed from the catalog (_updated_)                                                                                                                                                                                                      |
| 31  | Images (final)         | **Hybrid:** 7 hero products keep Google URLs · ~48 new get deterministic on-brand SVG placeholders · `onError` fallback retained                                                                                                                                                                                                                                                                       |
| 33  | Browsing               | **Store: working category filter tabs** (as designed) · **Builder panel: scroll** · no pagination                                                                                                                                                                                                                                                                                                      |
| 42  | Rent confirmation gate | **Demo-verify dialog** (extends C2) — clicking "Rent This Setup" opens a modal asking the user to type `this is a demo` (trimmed, case-insensitive match). On match: the confirmation renders from a receipt snapshot and the order is cleared from localStorage + memory (fresh cart; D1 re-applies on the builder). On mismatch: inline error + OK disabled. Cancel/Esc dismiss without side effects |

## Architecture & Engineering

| #   | Decision                   | Resolution                                                                                                                                                                                                                                                                                                                                                                                    |
| --- | -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 12  | TypeScript                 | **`strict: true` + `noUncheckedIndexedAccess` + zero `any`** — project-wide, every file inherits                                                                                                                                                                                                                                                                                              |
| 13  | Security                   | **Baseline hygiene + standard headers** (CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy) · `remotePatterns` allowlist (`lh3.googleusercontent.com`) · CSP font hosts (`fonts.googleapis.com`, `fonts.gstatic.com`) + analytics host (`va.vercel-scripts.com`) · no secrets in client · no `dangerouslySetInnerHTML` · compiled Tailwind (never CDN script) |
| 14  | Code style                 | **ESLint (`eslint-config-next`)** + Prettier + `prettier-plugin-tailwindcss` + husky/lint-staged + feature-boundary lint rules · `next build` as backstop                                                                                                                                                                                                                                     |
| 15  | Architecture               | **Feature-based vertical slices** (`features/home                                                                                                                                                                                                                                                                                                                                             | builder | store | summary`, barrel exports) + **shared layers** (`data/domain/state/hooks/ui/types/config/observability`) · **one component/hook/module per file** (ADR 0002) |
| 16  | Testing (unit/integration) | **Vitest + React Testing Library** — unit on `shared/domain`, integration on module combos, colocated `*.test.ts(x)`                                                                                                                                                                                                                                                                          |
| 17  | Tooling                    | **pnpm** · **Node 22 LTS** (`.nvmrc` + `engines`, raised from Node 20 — pnpm 11 requires ≥ 22.13)                                                                                                                                                                                                                                                                                             |
| 25  | Data delivery              | **Route handler `/api/products` + TanStack Query client fetch** · home = server component · builder/store/summary = client components                                                                                                                                                                                                                                                         |
| 26  | Error scope                | **Trio at app root:** `not-found.tsx` + `error.tsx` + `global-error.tsx`                                                                                                                                                                                                                                                                                                                      |
| 28  | Error page                 | **Generic copy + Try again (`reset`) + Back to Home + structured `logger.error`** — raw error never rendered                                                                                                                                                                                                                                                                                  |
| 29  | Loading                    | **Root `loading.tsx`** brand-styled skeleton for lazy-loaded route chunks                                                                                                                                                                                                                                                                                                                     |
| 32  | Seed generation            | **Generator script + curated hero overlay** (`scripts/generate-catalog.ts`) → committed `products.json`, unit-testable (ADR 0003)                                                                                                                                                                                                                                                             |
| 41  | Next.js version            | **Next.js 16 (Turbopack bundler)** — no third-party bundler; SWC compiles, Tailwind v4 Oxide compiles CSS                                                                                                                                                                                                                                                                                     |

## Testing — E2E

| #   | Decision         | Resolution                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 34  | E2E framework    | **Playwright (`@playwright/test`)**, Chromium-only, `webServer` auto-start (ADR 0005)                                                                                                                                                                                                                                                                                                                       |
| 35  | E2E scope        | **Full UI regression:** positive flows mimic real user interaction; **verification = displayed text** (items on confirmation + IDR total matches spec-computed sum); **11 negative flows** (N1 empty cart · N2 404 · N3 cap · N4 removal · N5 exclusivity · N6 partner exclusion · N7 image fallback · N8 API failure · N9 security headers · N10 corrupt storage recovery · N11 delivery input validation) |
| 36  | E2E run target   | **Local production build** (`next build && next start`); CI runs after unit/integration, gates merges                                                                                                                                                                                                                                                                                                       |
| 37  | E2E expectations | **Import `products.json` + spec-local arithmetic** — never import app's `pricing.ts`/`setupRules.ts`                                                                                                                                                                                                                                                                                                        |
| 38  | E2E location     | **`e2e/` at repo root** + `playwright.config.ts` at root — structurally distinct from colocated Vitest                                                                                                                                                                                                                                                                                                      |

## Validation — trust boundary (ADR 0004)

| #   | Decision                | Resolution                                                                                                                                                                                                                         |
| --- | ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G1  | localStorage hydration  | **Validate-and-fallback** — pure `validateSetupState` in `shared/domain` checks shape + business rules (quantities ≤ caps, IDs ∈ catalog, single chair/desk, no partner items); any failure → reset to defaults (D1 pre-selection) |
| G2  | Reducer enforcement     | **Reducer validates every action via `setupRules`** — invalid actions (over-cap, partner add) rejected as quiet no-ops                                                                                                             |
| G3  | Delivery Location rules | **Trim + non-empty + max 200 chars** (raised from 120 for the 2-line textarea); inline error (`aria-invalid`/`aria-describedby`); **Rent disabled until valid**                                                                    |
| G4  | E2E coverage            | **N10** (corrupt storage → defaults, no crash) · **N11** (empty/over-length → error + Rent disabled; valid → enabled)                                                                                                              |

## Exceptions (E1–E4)

| #   | Decision              | Resolution                                                                                                                                                                                                         |
| --- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| E1  | try/catch policy      | **Targeted at I/O boundaries only** (route handler, `localStorage` writes, async handlers); TanStack Query captures fetch errors natively; pure domain functions stay throw-only; error boundaries render outcomes |
| E2  | Route handler         | **Static import** (bundled → no runtime I/O failure) **+ contract guard** → generic 500 `{"error":"Products unavailable"}`                                                                                         |
| E3  | `localStorage` writes | **try/catch on `setItem`** → `console.warn` → degrade to in-memory (cart works for session); reads guarded by G1                                                                                                   |
| E4  | Last-mile capture     | **Global `unhandledrejection` + `error` listeners → structured log**                                                                                                                                               |

## Observability (O1–O4)

| #   | Decision       | Resolution                                                                                                                                                                                                                                            |
| --- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| O1  | Architecture   | **Vercel-native:** structured JSON logs (`logger.ts`) + `@vercel/speed-insights` (Web Vitals) + `@vercel/analytics` (traffic); zero backend, zero vendors (ADR 0006)                                                                                  |
| O2  | Event taxonomy | **Curated:** `info` — `rent.clicked` · `delivery.submitted` · `catalog.loaded` (`durationMs`) · `catalog.failed` · `warn` — `storage.degraded` · `validation.rejected` · `error` — `error.boundary` · unhandled rejections · `debug` — `cart.updated` |
| O3  | PII & CSP      | **PII-free by construction** — logger API has no address field (`delivery.submitted` logs `hasAddress`/`addressLength` only) · CSP adds `va.vercel-scripts.com` to `script-src`/`connect-src`                                                         |
| O4  | Traces         | **Lightweight:** timed structured-log events (`durationMs`, correlated `ts`) — no OTel, no vendor                                                                                                                                                     |

## Mockup conflicts ruled (C1–C6)

| #   | Conflict                               | Ruling                                                                                                                                                                                                                                                                                                                                                                                                    |
| --- | -------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | Summary's delivery fee / grand total   | Follow #7: line items + **Monthly Total** only                                                                                                                                                                                                                                                                                                                                                            |
| C2  | "Secure checkout via Stripe"           | **Demo-honest confirmation copy**; zero payment language                                                                                                                                                                                                                                                                                                                                                  |
| C3  | Summary zone overview (Garage/Outdoor) | **Removed from summary** — _superseded:_ the zone overview tiles were dropped (no impact: both tiles render "(Empty)" in the default flow); the zone concept still lives in the Builder canvas (Coffee Station / Relax Zone slots + Extras steppers)                                                                                                                                                      |
| C4  | Delivery Location input                | **Kept** — client-side, persisted, echoed into mock confirmation                                                                                                                                                                                                                                                                                                                                          |
| C5  | Home profile avatar                    | **Dropped** — no accounts                                                                                                                                                                                                                                                                                                                                                                                 |
| C6  | "Select your rental duration" copy     | **Rewritten** — "month-to-month, no long-term commitment"                                                                                                                                                                                                                                                                                                                                                 |
| C7  | Store card actions                     | **Removed** — the store is a display-only catalog gallery (mockup shows "Add to Setup" buttons; product decision to deviate); adding happens in the Builder's Selection Panel; partner card shows an informational "Partner Service" pill instead of the request flow                                                                                                                                     |
| C8  | Summary workspace preview              | **Removed + borderless receipt** — the left-side preview filler (mockup shows a workspace photo there) had no real content; the Order Summary is now the whole content: full-width, left-aligned, no card border/shadow. Delivery input is a 2-line textarea (true multi-line) and the Rent button is regular-width, right-aligned (both deviate from the mockup's single-line input + full-width button) |

## Change log

- **2025 — summary zone overview removed (supersedes C3):** `ZoneTiles` deleted from the summary page — the two tiles rendered "(Empty)" in the default flow and added no information about the built workspace. The zone concept remains in the Builder canvas (Coffee Station / Relax Zone slots, Extras steppers).
- **2025 — extras dropped from the catalog data (data):** the 4 extra/surfboard products were removed from products.json (generator template + curated hero deleted); the catalog is now 53 products (10/10/32/1 partner).
- **2025 — Extras category removed from builder + store (UX):** the builder's Extras tab (coffee/beanbag) and the store's Extras tab (surfboard family + partner motorcycle) are gone — the builder panel is Chairs/Desks/Accessories with coffee/beanbag managed on the canvas zones; the store browses Chairs/Desks/Accessories only. Extras + partner catalog data stays, just unlisted.
- **2025 — desk table persists on removal (fix):** the CSS table surface (bar + legs) is now always rendered on the canvas — removing the selected desk clears only its product image (+ an "Add a desk from the panel" hint on the table). Previously the empty state replaced the whole table.
- **2025 — chair/desk x placement fixed:** the × buttons floated in the object-contain letterbox margin (product SVGs are 4:3 but the boxes were 128x144 / 160x96). Boxes now match the 4:3 aspect (chair 192x144, desk 128x96) so the × sits on the product like the other cards.
- **2025 — chair/desk removable via x on the workspace (UX):** the selected chair (its row) and desk (its product image) gained top-left x buttons like the other product cards — clicking deselects (chairId/deskId → null). The deselect actions (deselectChair/deselectDesk) and the panel's Select/Deselect toggle were rebuilt (a working-tree reset had lost them), and D1 defaults now apply once per mount so removal is never re-seeded.
- **2025 — direct removal on canvas slots (UX):** filled monitor/lamp/plant/coffee/beanbag slots now show a small × button (top-left, opposite the qty badge) that decrements by one and empties the slot at quantity 1 — no more digging through the panel steppers to remove. The filled slot was restructured into a positioned container + add button + × button (no nested buttons).
- **2025 — stepper buttons circular (UX):** the panel's + / - buttons switched from rounded pills to h-8 w-8 circles (new Button size="circle"), matching the home page's circular add button; the shared Button now owns the radius per size (deterministic).
- **2025 — home page pre-footer spacing (fix):** HowItWorks used py-section-gap (80px bottom padding) which stacked with the footer margin — 136px of dead space before the footer. Bottom padding trimmed to pb-4 → 72px gap.
- **2025 — home + store bottom spacing (fix):** same treatment as summary — removed min-h-screen from the wrappers (home also dropped its mobile-only h-24 spacer) and store's main pb-32 → pb-4; the footer now follows the content with ~32-48px gaps.
- **2025 — summary bottom spacing (fix):** the summary page's min-h-screen wrapper stretched short content to the viewport height, leaving ~259px between the Rent button and the footer; the wrapper now hugs its content (footer gap 72px) and the root body carries bg-background.
- **2025 — footer with git version (add):** a SiteFooter renders "Core Rental · v<tag>" centered on every page. The version is sourced from the git semantic version at build time (scripts/version.ts runs `git describe --tags` via a prebuild hook, falls back to package.json) into the committed src/shared/config/version.ts.
- **2025 — canvas height tightened (fix):** the workspace canvas inner flex dropped from h-[440px] to h-[300px] — ~140px of dead space above the monitor removed (201px → 61px, just the card padding); all slots keep their relative layout with zero overlaps.
- **2025 — filled canvas slot positioning (fix):** the filled monitor/lamp/plant slot carried both `relative` and `absolute` classes; `.relative` won the cascade and the selected item jumped to the top-right. `relative` is now zone-only — canvas slots stay absolutely positioned at their slot.
- **2025 — Select/Selected full-width (fix):** the panel's Select/Selected buttons now span the full card row (w-full), matching the stepper row — no dead space on the right.
- **2025 — Select/Selected buttons compacted (fix):** the chairs/desks Select buttons were CTA-sized (px-6 py-3) in the compact cards; they now use size="sm" like the stepper buttons.
- **2025 — builder panel tabs icon-only (UX):** the Selection Panel's stacked category buttons became a horizontal icon-only tab strip (chair/desk/keyboard/beach_access); the category name appears as a hover tooltip and via aria-label (accessible + testable).
- **2025 — Add Monitor slot moved down (fix):** the monitor sat at the canvas top, ~192px above the desk cluster. It now sits centered just above the desk (bottom-36, 24px gap), horizontally between the lamp and plant — a 'monitor on the desk' arrangement.
- **2025 — Add Lamp slot relocated (fix):** the lamp sat at right-40 and overlapped the centered desk product image (96px×96px). It now sits at left-12 — the bottom row reads Lamp | Desk | Plant, all clear with ~16px gaps.
- **2025 — desk product image on the table (add):** the selected desk's product image now renders overlapping the canvas table surface (16px vertical overlap, centered) — the workspace shows both the table layout and which desk is selected; the chair stays in its own row below.
- **2025 — chair relocated on the builder canvas (fix):** the chair no longer overlays the desk — it now sits in its own centered row above the Coffee Station / Relax Zone tiles; the desk is back to the mockup's table-layout surface (slightly deepened wood tone so it reads).
- **2025 — builder desk representation (fix):** the desk was a near-invisible CSS beige bar (1.72:1 contrast on the white canvas) hidden under the chair; it now renders the selected desk's product image behind the chair, symmetric with the chair's image treatment.
- **2025 — demo verification gate on Rent (new #42):** "Rent This Setup" now opens a dismissable dialog requiring the phrase `this is a demo` (trimmed, case-insensitive). On match the order is cleared from localStorage + memory and the confirmation renders from a receipt snapshot; mismatch shows an inline error with the OK button disabled.
- **2025 — summary receipt final layout (C8):** the Order Summary is now borderless, full-width, left-aligned (no card box); Delivery Location became a 2-line true-multiline textarea with the G3 cap raised 120 → 200; the Rent button is regular-width and right-aligned.
- **2025 — store → catalog gallery (supersedes #20):** removed the Add to Setup / Request Rental buttons and the partner request modal from the store; cards are display-only with an optional image badge (partner pill). Adding happens exclusively in the Builder's Selection Panel. N6 (partner never in cart) unchanged. Tests reworked: `StoreCard`/`StoreGrid` unit tests assert display-only rendering; `e2e/store.spec.ts` browses by category and asserts no add/request affordances.

## Interpretations & platform notes

- **"One class per file"** → one _unit_ per file (component/hook/module — React is functional)
- **"Each file uses strict"** → tsconfig-level strictness, inherited by all files (no per-file toggle exists)
- **Vercel** → deployment _platform_, not a dependency: zero-config Next.js deploy, no `vercel.json`, no `vercel` package; headers run via `next.config.ts` on Vercel's runtime
- **Bundling** → no third-party bundler: **Turbopack** bundles, **SWC** compiles/minifies, Tailwind v4 Oxide compiles CSS; Vitest's Vite is test-time only
- **Routing** → App Router file-based (`src/app/` = route table); `<Link>` for nav + `router.push()` for programmatic; code-split lazy loading with `loading.tsx`; cart context in root layout survives navigation; TanStack Query cache survives navigation
- **Test files in production** → bundler reachability guarantees colocated `*.test.ts(x)` never ship; type-checked at build, emitted never

## Repository structure (proposed — not yet scaffolded)

See `specs/tech-architecture/tech-stack.md` § Repository Structure for the full tree.

- **2025 — replace-always monitor select when full (supersedes the Q3 no-op):** with all 3 monitor slots filled, selecting ANY monitor in the panel — including one already placed — replaces the most-recently-added card (e.g. [A,B,C] + select A → [A,B,A], so 2-of-a-model is reachable from a full row). The only quiet no-op left is selecting the model that is already the most recent (identical value). The swapped card always pulses briefly (Q2 unchanged). Ruling recorded as superseding the e09s02 Q3 "no-op if already placed".
- **2025 — selection panel keyword search (feat):** each tab (chairs/desks/accessories) has its own search input filtering products by name OR description — case-insensitive substring (Q3 ruling). Empty results: chairs/desks show "No products match"; the accessory tab keeps every subcategory header and shows "No {monitor|lamp|plant} match" under empty groups (Q2=2). A × button clears the input and Escape resets (Q4). Per-tab keywords persist when switching tabs (Q1=2). Coffee/beanbag are canvas zones only — unaffected. 162 unit + 21 E2E green.
- **2025 — logger events sent to Vercel Web Analytics (feat):** every taxonomy event (all 10: rent.clicked, delivery.submitted, storage.degraded, error.boundary, unhandledrejection, unhandlederror, catalog.loaded, catalog.failed, validation.rejected, cart.updated) now ALSO calls `track()` — dots normalized to underscores (rent_clicked, catalog_loaded…) — with the SAME sanitized payload (PII guard applies before track). Best-effort (try/catch, fire-and-forget). Newly wired call sites: catalog.loaded/failed (products hook, durationMs), validation.rejected (hydration trust boundary), cart.updated (cart provider, { items, total }, only on content changes — not delivery typing or mount). Tests: @vercel/analytics mocked globally in setup.ts; logger.test asserts forwarding/sanitization/best-effort.
- **2025 — pointer cursor on clickable elements (fix):** Tailwind v4's preflight dropped the v3 `cursor: pointer` on buttons, so every interactive element showed the default arrow on hover (Home/Builder/Summary). Added a global `@layer base` rule: enabled buttons, `[role="button"]`, `a[href]`, `[role="link"/"tab"/"menuitem"/"radio"/"checkbox"]`, `label[for]`, `select`, `summary` → `cursor: pointer`. Text inputs keep the I-beam (typing affordance); disabled controls stay inert (`:not(:disabled)`). E2E asserts computed cursors (button/link pointer, searchbox text).
- **2025 — Misc group in the Accessories tab (feat):** coffee + beanbag are now visible in the Selection Panel under a combined "misc" subcategory (after monitor/lamp/plant) with QuantityStepper controls — in sync with the Coffee Station / Relax Zone canvas zones (same cart state, caps coffee=1 / beanbag≤2). Catalog expanded: +5 coffee (9 generated + 1 hero = 10) and +5 beanbag (9 + 1 = 10) Bali-named products → 63 total (42 accessories). Count assertions updated (32 → 42).
- **2025 — filled zone click increments the selected product (fix):** CanvasSlot.onAdd always dispatched addAccessory(products[0]) — clicking a filled Coffee Station / Relax Zone (and lamp/plant) card added the FIRST product of the cap key, swapping the displayed product instead of incrementing. Now targets `selected ?? target`: filled slots increment the displayed product (reducer quiet-no-ops over cap, G2) and the Max label shows at cap; empty slots still fall back to the first product. 168 unit + 24 E2E green.
- **2025 — panel tab order + product-list border (fix):** Selection Panel tabs reordered desk → chair → accessories (desk is now the default tab, matching the order); the product list's top border (outline-variant #bcc9c8) removed entirely so it blends with the #f8f9fa background. Tests updated for the new default tab (unit + E2E activate the Chairs tab where chairs are asserted). 169 unit + 24 E2E green.
- **2025 — coffee/beanbag single-select + max 1 (fix):** beanbag cap lowered 2 → 1 (coffee was already 1). The Misc group's coffee/beanbag switched from QuantitySteppers to Select/Deselect buttons with replace semantics — clicking another machine REPLACES the zone's selected one (new reducer action `replaceExclusiveAccessory`: clears same-capKey siblings, sets the target to 1); Deselect removes it (zone returns to "Add Machine"/"Add Bean Bag"). Zones unchanged (Max label at 1; over-cap clicks no-op). 174 unit + 24 E2E green.
- **2025 — lamp/plant single-select + max 1 (fix):** lamp cap 2 → 1 and plant cap 4 → 1; the panel's lamp/plant groups switched from QuantitySteppers to Select/Deselect with replace semantics (clicking another lamp/plant replaces the zone's current one via replaceExclusiveAccessory; Deselect removes). All panel accessories are now single-select (monitor = 3-slot model unchanged); the now-dead QuantityStepper component, its test, and its export were removed. 172 unit + 24 E2E green.
- **2025 — cart-validity UX gates (fix):** a cart is rentable only when BOTH a chair and a desk are selected (`hasSeating` in setupRules). When invalid: the header bag icon disables (aria-disabled span, no navigation, dimmed) and the builder's "View Setup Summary" CTA disables (disabled button). To make the header see the cart, CartProvider moved global (providers.tsx, catalog fetched once); per-page providers removed; the write-through is gated on the catalog being loaded so a seeded cart is never clobbered before hydration. The canvas hints renamed to "Add Desk *" / "Add Chair *" with the required-marker asterisk in the error red (#ba1a1a). 177 unit + 24 E2E green.
- **2025 — code cleanup (refactor):** removed dead code accumulated across the UX iterations — `defaultSelection` (duplicate of `defaultsIfEmpty`) + its tests, `SKU_CODE_TO_CATEGORY`, `useBuilderState` + its barrel export, the `.icon-fill` CSS class, the unused `Button size="circle"`, and the vestigial `extra`/`EXT` schema (QUANTITY_CAPS.extra, SKU_CODE_TO_CAP.EXT, generator/script code tables — the catalog has no extra products since surfboards were removed; ProductCategory keeps "extra"). CanvasSlot's now-dead `capKey` prop (the Surfboard label check) was removed too, and the internal-only e2e `productBySku` helper was de-exported. Catalog regeneration byte-identical. 174 unit + 24 E2E green.
- **2025 — hide the quantity badge at qty 1 (fix):** filled zone cards (lamp/plant/coffee/beanbag — always max 1 now) no longer render the `×1` pill, matching the monitor/chair/desk cards. Per Q1=2 the accessible name also drops the quantity at 1 ("Coffee Station" not "Coffee Station: 1"); matchers updated (E2E uses exact:true since "Coffee Station" substrings "Remove Coffee Station"). 174 unit + 24 E2E green.
- **2025 — AI Workspace Designer (e10) session rulings (planning, not built):** (1) **Provider-switchable LLM route** — Vercel AI SDK; `AI_BASE_URL` set → LM Studio OpenAI-compatible (`localhost:1234/v1`, officially listed provider) in dev, else OpenAI API in prod (`AI_MODEL` selects; prod default gpt-4.1-mini). (2) **Best-fit + explain output**: strict JSON mapped onto the rigid builder schema (1 chair, 1 desk, ≤3 monitors, ≤1 each coffee/beanbag/lamp/plant), empty slots allowed, server-side SKU-exists/slot/cap validation, auto-retry once, plain-language note. (3) **Pure-tool discovery**: catalog NOT in the prompt; the AI calls tools (searchCatalog / getSetupTotal / finalizeDesign) so every SKU is real. (4) **Budget**: monthly IDR ceiling (30 juta = Rp 30.000.000/mo), tool-checked + server-re-verified; impossible → honest refusal with cheapest alternatives; optional unless stated. (5) **Apply = replace + confirm** (non-empty cart), via existing reducers — no new SetupState fields. (6) **Panel on builder page**: preview card + reasoning trail, 60s timeout/cancel, non-streaming v1. (7) **Demo-lite guardrails**: env gate (503), in-memory rate limit ~10/10min/IP (429), output cap; key server-side only. (8) **Observability facts-only**: ai.request {model,durationMs,toolCalls,ok} + ai.design_applied {skus,total} — prompt text never emitted (PII-safe by construction). (9) Dev model default gpt-oss-20b (devstral-small-2-2512 alternative; all four local models tool-capable). (10) "Computer" = monitor — no catalog gap. (11) Epic e10, wsjf 6.0, v1.12.0 minor, 3 stories / 19 BCPs / 16 tasks; deps ai@7.0.84 + @ai-sdk/openai@4.0.51 + zod verified. (12) TDD with mocked LLM, E2E with stubbed route, preflight+CI green, threat model first; NOT pushed until the user's release signal.
- **2025 — e10 AI workflow rewrite (session rulings Q1–Q4, NOT pushed):** (1) The API still returns ONE validated design — "list top 3 combinations" is the model's internal reasoning; the panel/contract are unchanged. (2) Off-topic queries ("what's the weather") are gated by the LLM via a new `rejectQuery` tool; the response is a 200 `{ rejection: { message } }` with the message STANDARDIZED server-side to "query not about workspace building" (the model cannot vary it); the panel shows a distinct rejected state + Regenerate. (3) The system prompt now mandates a workflow: for EACH of the 7 types (desk/chair/monitor/lamp/plant/bean bag/coffee machine) call searchCatalog with the query, rank + keep top 5 per type (≥35 candidates) — enforced as prompt guidance, NOT server-verifiable (the final design carries ≤13 skus); the server still validates only the final design. Then build top 3 combos (1 desk, 1 chair, ≤3 monitors, ≤1 each accessory) matching the query and randomly pick ONE. (4) Budget unchanged (extractBudgetIdr + getSetupTotal + server re-verification + honest refusal); changes amend the unreleased e10 branch (still v1.12.0); system prompt written fresh from the workflow bullets.
- **2025 — /api/product endpoint — NOT BUILT (grilling ruling Q1=a):** the front end already fetches the catalog from `GET /api/products` (useProducts) and never bundles product.json client-side (verified in the built chunks). The AI route imports products.json in-process server-side — the correct pattern (a serverless route calling its own API adds latency + a failure mode for zero benefit; the catalog is committed + byte-stable, not runtime-mutable). A singular `/api/product/:skuNo` endpoint would be YAGNI (no consumer). Architecture confirmed as-is; no code change.
- **2025 — subCategory field + filtered /api/products — NOT BUILT (grilling ruling Q1=a, Q2 abandoned):** a `subCategory` property (monitor/lamp/plant/coffee/beanbag, accessories-only) and `category`/`subCategory` query params on `/api/products` were scoped and grilled; Q1 ruled the existing domain word "coffee" (not "machinecoffee"), then the whole requirement was dropped (Q2: no consumer for the filtered endpoint — the panel/store filter a 63-product catalog client-side; the AI route stays in-process per the earlier ruling). No code change; catalog + endpoint unchanged.
- **2025 — subCategory + searchCatalog rewrite (session rulings Q1–Q3, NOT pushed):** (1) `Product.subCategory` added — `monitor | lamp | plant | coffee | beanbag` on accessory products, `null` on chair/desk/partner; value "coffee" (domain word, not "machinecoffee"); generator + curated-hero emit it (byte-stable); `isProduct`/`isValidCatalog` require it AND enforce consistency with the sku prefix (single source of truth: the prefix; mismatch → catalog rejected, E2 fallback); `/api/products` unchanged (field rides in the payload); client Product type updated. (2) `searchCatalog` tool signature: `type` REPLACED by `category` (chair|desk|accessory) + `subCategory` (monitor|lamp|plant|coffee|beanbag; implies accessory — loose pairing); both omitted → full catalog; invalid enums → empty result; `query`/`maxPrice` stay; `TYPE_PREFIX` removed. (3) **LLM-as-ranker**: searchCatalog is now a pure RETRIEVER (returns ALL matches, no top-5 slice); the system prompt instructs the model to rank candidates by closeness to the query and keep the top 5 per type (≥35 across 7 types); top-3 combos → random pick unchanged. (4) Three scenario E2E tests added (fancy college full setup / minimal Rp 1 jt exactly at budget / gaming under Rp 20 jt), stubbed designs, CI-safe.
- **2025 — AI workflow rulings (Qwen3 verification session, NOT pushed):** (1) NEW RULING superseding top-5/35: keep the **top 2 per type (≥14 candidates total)** — user's prompt edit (1.1/1.2 structure). (2) **Empty-result fallback** (the verified fix): if a type search with the user's query returns an empty list, search that type again WITHOUT the query — never repeat an empty search (the catalog's descriptions rarely contain the query words, e.g. only 1/63 mention "gaming" — hard query-filtering caused infinite re-search loops). (3) **Adapter bug fixed**: AI SDK v7 ToolResult exposes arguments as `input`, not `args` — `resolveToolOutcome` now reads either (the model WAS finalizing; the adapter was blind to it). (4) Step cap 8 → 14 (8 was mathematically tighter than the 7-search workflow). (5) Real-LLM verification SUCCEEDED with **Qwen3-Coder-30B-A3B-Instruct** (LM Studio :8080): "high gaming workspace under Rp 20 jt" → validated design, total Rp 4.155.000 ≤ 20jt, in ~60s. (6) **Gated real-LLM E2E scenario** added (probes LM Studio reachability, skips in CI, asserts design + budget ≤ 20jt) — passes locally. (7) gpt-oss-20b was NOT the problem — the empty-results loop + the `input`/`args` bug broke it too.
- **2025 — gpt-oss-20b verification verdict (NOT pushed):** reran the gaming query with gpt-oss-20b after the input/args + empty-fallback + step-cap fixes. It now FOLLOWS the workflow (per-type searches with fallbacks, getSetupTotal calls) but **never finalizes reliably**: corrupted tool args (truncated JSON), `finishReason: length` truncations, and 13–29s variance across runs. Step cap raised 14 → 20 (room to finish); still fails. **Qwen3-Coder-30B-A3B-Instruct is the verified local model** (4/4 completions, 3–4 steps, clean args, ~60s). Local model choice for the AI dev flow: Qwen3-Coder-30B (recommended); gpt-oss-20b not viable for the 7-type tool workflow on this setup. Production (OpenAI) remains gpt-4.1-mini.
- **2025 — AI speed optimization (NOT pushed):** traced the 57s completion: 5 roundtrips, 21 redundant searchCatalog calls (3 sweeps), context 1.1k→8.4k tokens (time scales with context), a getSetupTotal roundtrip, and a strict LLM-total-mismatch rejection that cost a retry. Fixes: (1) one-sweep prompt — all 7 searches in the FIRST message, retry empty ones immediately, NEVER search again → 3 steps; (2) lean retriever — ≤8 hits/search, 60-char descriptions → context 4.3k; (3) **getSetupTotal removed** (the route recomputes totals server-side; the tool was redundant); (4) LLM-reported totalPerMonth is advisory — validateDesign tolerates mismatches (the computed total is authoritative for budget + response). Result: **57s → ~26s warm** (gated E2E 59.7s → 32.9s). Cold first-call after a model swap still ~60s (model load — not code).
- **2025 — searchCatalog = pure category/subCategory retriever (new ruling, NOT pushed):** the `query` filter was useless (literal substring match — only 1/63 products mentions "gaming"; 6 of 7 step-0 calls returned [] and starved the LLM-as-ranker). searchCatalog now filters ONLY by `category` + `subCategory` (query + maxPrice removed from the schema/args/tests); every valid combo is guaranteed non-empty (each type has 6–10 products); the LLM does all query-relevance ranking. Prompt: all-7-type searches in the first message (every call returns candidates — the empty-fallback sentence is gone). Verified with Qwen3: "gaming workspace" → 3× UHD monitors ranked semantically, Rp 3.85M ≤ 20jt; gated E2E ~28s. Observation: the model can pick 3 identical monitors (valid per caps) — a "prefer a mix" prompt tweak is a possible future polish.
