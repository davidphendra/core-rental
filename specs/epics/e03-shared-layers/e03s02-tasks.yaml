story_id: e03s02
title: Cart state & persistence
status: passing
bcps: 4
tasks:
  - id: 1
    description: "shared/state/BuilderStore.tsx: context + reducer; every action validated
      via setupRules, invalid actions rejected as quiet no-ops (G2)"
    verify: pnpm test -- BuilderStore.test.tsx
    risk: P0
    status: passing
    security: low
    allure:
      severity: critical
      categories:
        - unit
        - domain
        - Security Review
  - id: 2
    description: "shared/state/useLocalStorage.ts: reads via validateSetupState (fallback
      to defaults), writes try/catch QuotaExceeded -> warn + in-memory degrade (E3)"
    verify: pnpm test -- useLocalStorage.test.ts
    risk: P0
    status: passing
    security: low
    allure:
      severity: critical
      categories:
        - unit
        - domain
  - id: 3
    description: "shared/hooks/useBuilderStore.ts: React boundary over BuilderStore;
      SetupState type in shared/types/setup.ts"
    verify: pnpm typecheck
    risk: P2
    status: passing
    allure:
      severity: normal
      categories:
        - types
