story_id: e09s02
title: Three monitor slots
status: todo
bcps: 8
tasks:
  - id: 1
    description: >-
      State: SetupState gains monitorSlots: [string|null, string|null,
      string|null]; reducer actions selectMonitor (fill-first-empty /
      replace-most-recently-added / no-op if already placed) and
      removeMonitorSlot(index); EMPTY_SETUP updated; monitor keys leave
      quantities
    verify: pnpm test -- BuilderStore.test.tsx
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - unit
        - state
        - domain
  - id: 2
    description: "Validation: monitorSlots validated (3 entries, null or valid skuNo);
      quantities can no longer contain monitor keys; hydration path handles both"
    verify: pnpm test -- validateSetupState.test.ts
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - unit
        - security
  - id: 3
    description: "Cart/line items: CartProvider/useCartTotals derive monitor line
      items from slots (grouped by skuNo, duplicates allowed); Summary renders them"
    verify: pnpm test -- summary-page.test.tsx
    risk: P1
    status: passing
    allure:
      severity: high
      categories:
        - unit
        - ui
  - id: 4
    description: "Canvas: 3 compact monitor slots in a centered row above the desk;
      empty slot adds the first catalog monitor; filled slot shows image + × to
      clear; replace-most-recent shows a brief highlight on the swapped card"
    verify: pnpm test -- BuilderCanvas.test.tsx BuilderCanvas.a11y.test.tsx
    risk: P1
    status: passing
    allure:
      severity: high
      categories:
        - unit
        - ui
  - id: 5
    description: "Panel: monitor products switch from QuantityStepper to a Select
      button (fill-first-empty / replace-most-recent / no-op-if-placed); other
      accessories keep steppers"
    verify: pnpm test -- SelectionPanel.test.tsx
    risk: P1
    status: passing
    allure:
      severity: high
      categories:
        - unit
        - ui
  - id: 6
    description: "E2E: builder + summary cover 3-slot combos (2A+1B, 3C), full-slot
      replace-most-recent, no-op on already-placed, × removal, line items"
    verify: pnpm build && pnpm test:e2e
    risk: P1
    status: passing
    allure:
      severity: high
      categories:
        - e2e
